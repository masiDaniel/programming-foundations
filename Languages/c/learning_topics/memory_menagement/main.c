#include <stdio.h>
#include <stdlib.h>


int main(){

    int students[20];
    printf("%lu", sizeof(students));

    // int *pointer1 = malloc(size);
    // int *pointer2 = calloc(amount, size);



    return 0;
}